@using Esas
@namespace Bileşenler

@for (int kaçıncı_yorum = 0; kaçıncı_yorum < gösterilecek_sözler.Length; kaçıncı_yorum++)
{
    <TekYorum gösterilecek_söz=@gösterilecek_sözler[kaçıncı_yorum]/><br/>
}

@code {
    [Parameter]
    public Esas.söz[] gösterilecek_sözler {get; set;}
}